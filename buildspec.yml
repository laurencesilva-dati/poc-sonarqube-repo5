version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing Sonar Scanner"
      - npm install -g sonar-scanner
  build:
    commands:
      - echo "Running SonarQube analysis"
      - sonar-scanner -Dsonar.projectKey=$SONAR_PROJECT_KEY -Dsonar.sources=. -Dsonar.host.url=$SONAR_HOST_URL -Dsonar.login=sqp_7e23d1d3104350f9cb7052fb2b3241ab89d45fe1
      - echo "Sonarqube scan completed"